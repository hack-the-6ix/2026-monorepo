import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs/blocks';
import cn from 'classnames';
import './index.css';
 
<Meta title="Atoms/Shadows" />

<h1 className="text-5xl font-bold pb-6!">Drop Shadows</h1>

<ul className="grid grid-cols-3 gap-6 list-none! p-0! m-0!">
  {['shadow-none', 'shadow-xs', 'shadow-sm', 'shadow-md', 'shadow-lg', 'shadow-xl'].map((shadow) => (
    <li className="flex flex-col gap-1 m-0!" key={shadow}>
      <div className={cn(shadow, 'p-6 bg-white outline-2 outline-neutral-500 rounded-sm')}/>
      <p className="font-sans text-xs font-semibold text-neutral-500!">{shadow}</p>
    </li>
  ))}
</ul>
