HT6_API_URL=http://localhost:3000/api
NEXT_PUBLIC_HT6_API_URL=/api/ht6
HOST_URL=http://localhost:3001
NEXT_PUBLIC_SEASON_CODE=S26

# Hardware portal backend (deployed from the hardware portal repo).
# Client must stay same-origin (/api/hardware) so dash cookies are sent;
# the Next.js proxy then forwards to HARDWARE_API_URL.
HARDWARE_API_URL=https://api.hardware.hackthe6ix.com
NEXT_PUBLIC_HARDWARE_API_URL=/api/hardware
