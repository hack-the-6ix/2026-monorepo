import ApplicationsClosed from '@/components/ApplicationsClosed';

export default function WelcomePage() {
  return (
    <main className="relative flex min-h-screen flex-col items-center justify-center overflow-hidden pt-[28vh] md:pt-0">
      <ApplicationsClosed />
    </main>
  );
}
